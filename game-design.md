# game-design.md

## 0. 문서 목적

이 문서는 `game-plan.md`의 **아쿠아 구피 키우기 미니게임**을 HTML, CSS, 순수 JavaScript만으로 구현하기 위한 개발 설계서이다.

개발 경험이 적은 사람도 구현 순서를 따라갈 수 있도록 복잡한 구조보다 다음 원칙을 우선한다.

- 한 화면에서 게임이 진행되는 정적 웹 게임
- 서버 없이 실행
- GitHub Pages에서 바로 배포 가능
- 외부 라이브러리 없이 구현
- 복잡한 물리 엔진이나 경로 탐색 사용 금지
- HTML 요소와 JavaScript 객체를 중심으로 단순하게 구성
- 1-1부터 1-3 엔딩까지 완주 가능한 상태를 먼저 구현
- 세부 연출보다 게임 규칙의 정상 작동을 우선

---

# 1. 개발 방식과 제한 조건

## 1.1 사용 기술

| 구분 | 사용 기술 | 역할 |
|---|---|---|
| 화면 구조 | HTML5 | 게임 화면, UI, 오버레이 구성 |
| 디자인 | CSS3 | 배치, 크기, 애니메이션, 반응형 화면 |
| 게임 로직 | Vanilla JavaScript | 이동, 충돌, 상태, 재화, 구매, 스테이지 진행 |
| 이미지 | PNG | 배경, 캐릭터, 아이템, UI, 이펙트 |
| 오디오 | 브라우저 Audio | 효과음 및 선택적 BGM |
| 배포 | GitHub Pages | 정적 웹게임 공개 |

외부 프레임워크와 설치형 라이브러리는 사용하지 않는다.

사용하지 않는 것:

- React
- Vue
- Phaser
- PixiJS
- jQuery
- Node.js 서버
- 데이터베이스
- 회원가입
- 로그인
- 결제
- 서버 저장
- 별도 빌드 도구

---

## 1.2 기본 개발 방식

게임 전체를 하나의 HTML 페이지 안에서 실행한다.

게임 오브젝트는 JavaScript 객체로 정보를 관리하고, 화면에는 필요한 HTML 요소를 생성해 표시한다.

예:

- 구피 정보 → JavaScript 객체
- 화면의 구피 이미지 → `<div>` 또는 `<img>`
- 재화 정보 → JavaScript 객체
- 화면의 동전 → `<img>`

매 프레임 또는 짧은 주기마다 다음 내용을 갱신한다.

1. 물고기 위치
2. 목표 위치
3. 사료 탐색
4. 충돌 여부
5. 허기 시간
6. 재화 생성 시간
7. 에일리언 이동
8. 화면 표시

---

## 1.3 게임 루프

게임의 움직임은 `requestAnimationFrame` 기반의 하나의 게임 루프로 처리하는 것을 권장한다.

게임 루프의 역할:

1. 현재 시간과 이전 프레임 사이의 시간 차이 계산
2. 게임 속도 배율 적용
3. 각 개체 상태 업데이트
4. 위치 이동
5. 충돌 검사
6. 사망/삭제 검사
7. 화면 위치 반영

초보자에게 중요한 점:

각 캐릭터마다 별도의 무한 타이머를 많이 만드는 것보다 **하나의 게임 루프에서 전체 개체를 갱신하는 방식이 관리하기 쉽다.**

---

## 1.4 좌표 기준

게임 로직의 좌표는 수족관 플레이 영역을 기준으로 한다.

권장 방식:

- 수족관 왼쪽 상단: `(0, 0)`
- 오른쪽 방향: X 증가
- 아래쪽 방향: Y 증가

캐릭터가 화면 밖으로 나가지 않도록 이동 가능한 범위를 제한한다.

예:

- 상단 UI 영역 제외
- 하단 모래 바닥보다 위쪽까지만 물고기 이동 가능
- 조개는 하단 고정

---

## 1.5 제한 조건

### 제작 시간
약 2시간을 목표로 한다.

따라서 다음 기능은 단순화한다.

- 물리 엔진 사용하지 않음
- 복잡한 충돌 박스 사용하지 않음
- 네비게이션 AI 사용하지 않음
- 물고기의 이동 경로는 직선 이동
- 이동 목표는 랜덤 좌표 사용
- 충돌은 중심점 거리 또는 단순 사각형으로 판정
- 세밀한 애니메이션 상태 머신은 만들지 않음

---

# 2. 프로젝트 파일 및 폴더 구조

GitHub Pages에서 바로 실행할 수 있도록 모든 파일을 상대 경로로 연결한다.

권장 구조:

```text
aquarium-game/
│
├─ index.html
├─ README.md
│
├─ css/
│  └─ style.css
│
├─ js/
│  ├─ main.js
│  ├─ game.js
│  ├─ entities.js
│  └─ config.js
│
├─ assets/
│  ├─ images/
│  │  ├─ background/
│  │  │  ├─ Background_Stage.png
│  │  │  └─ Background_Hatchery.png
│  │  │
│  │  ├─ fish/
│  │  │  ├─ Guppy_Default.png
│  │  │  ├─ Guppy_Starvation.png
│  │  │  ├─ Guppy_Dead.png
│  │  │  ├─ Piranha_Default.png
│  │  │  ├─ Piranha_Starvation.png
│  │  │  └─ Piranha_Dead.png
│  │  │
│  │  ├─ alien/
│  │  │  ├─ Alien.png
│  │  │  ├─ Alien_Portal.png
│  │  │  ├─ Effect_Carnivore.png
│  │  │  └─ Effect_Hit.png
│  │  │
│  │  ├─ pets/
│  │  │  ├─ Clam_Default.png
│  │  │  ├─ Clam_Open.png
│  │  │  ├─ StripedMarlinDefault.png
│  │  │  └─ StripedMarlin_Anger.png
│  │  │
│  │  ├─ items/
│  │  │  ├─ Egg.png
│  │  │  ├─ Prey.png
│  │  │  ├─ Money_Diamond.png
│  │  │  ├─ Money_GoldCoin.png
│  │  │  ├─ Money_Pearl.png
│  │  │  └─ Money_SilverCoin.png
│  │  │
│  │  └─ ui/
│  │     ├─ UI_ItemSlot.png
│  │     └─ UI_MoneySlot.png
│  │
│  └─ sounds/
│     ├─ feed.mp3
│     ├─ collect.mp3
│     ├─ buy.mp3
│     ├─ warning.mp3
│     ├─ hit.mp3
│     └─ gameover.mp3
│
└─ game-plan.md
```

---

## 2.1 파일별 역할

### `index.html`
게임에 필요한 화면 영역만 작성한다.

포함 대상:

- 게임 전체 컨테이너
- 상단 구매 UI
- 재화 UI
- 수족관 플레이 영역
- 스테이지 표시
- 안내 메시지
- 게임오버 오버레이
- 부화장 화면
- 엔딩 화면

---

### `css/style.css`

담당 기능:

- 전체 게임 화면 크기
- 수족관 배경
- UI 배치
- 물고기 이미지 크기
- 좌우 반전
- 재화 크기
- 버튼 효과
- 잠금 표시
- 게임오버 오버레이
- 모바일 반응형
- 스프라이트 시트 애니메이션

---

### `js/config.js`

게임 규칙에 사용되는 고정 값을 모아 둔다.

예시 항목:

- 초기 재화
- 사료 가격
- 구피 가격
- 피라냐 가격
- 알 가격
- 재화 가치
- 에일리언 HP
- 이동 속도
- 허기 시간
- 성장에 필요한 먹이 수
- 스테이지별 설정

초보자는 숫자를 여러 파일에 직접 작성하지 말고 한곳에서 관리하는 것이 좋다.

---

### `js/entities.js`

게임에 등장하는 개체의 생성 규칙을 담당한다.

대상:

- 구피
- 피라냐
- 사료
- 재화
- 에일리언
- 조개
- 청새치
- 포탈
- 이펙트

---

### `js/game.js`

실제 게임 규칙을 담당한다.

주요 역할:

- 게임 초기화
- 스테이지 시작
- 게임 루프
- 이동
- 먹이 탐색
- 충돌
- 성장
- 허기
- 재화 드롭
- 구매
- 에일리언
- 게임오버
- 스테이지 클리어

---

### `js/main.js`

웹 페이지가 준비된 뒤 게임을 시작하는 진입점이다.

담당 기능:

- HTML 요소 찾기
- 이미지 로딩 확인
- 이벤트 등록
- 게임 초기화 호출

---

# 3. 화면 영역별 구성 요소

전체 화면을 크게 다음 영역으로 나눈다.

```text
┌─────────────────────────────────────┐
│ 구매 UI                    재화 UI │
├─────────────────────────────────────┤
│                                     │
│                                     │
│          수족관 플레이 영역          │
│                                     │
│                                     │
├─────────────────────────────────────┤
│ 스테이지      안내 메시지            │
└─────────────────────────────────────┘
```

---

## 3.1 전체 게임 컨테이너

역할:

- 모든 UI와 게임 개체를 포함
- PC에서는 일정 비율 유지
- 모바일에서는 화면 너비에 맞게 축소

권장 화면 비율:

**16:9**

예:

- 기준 해상도: 1280 × 720
- 실제 브라우저에서는 비율을 유지하며 확대/축소

---

## 3.2 상단 구매 영역

왼쪽부터:

1. 구피
2. 사료 상한
3. 피라냐
4. 알

각 슬롯에 표시할 정보:

- 아이템 이미지
- 가격
- 잠금 상태
- 필요하면 현재 단계

잠금 상태에서는:

- 슬롯을 어둡게 표시
- 클릭 불가
- 자물쇠 또는 반투명 효과

---

## 3.3 우측 상단 재화 UI

표시 값:

`현재 재화 $`

예:

`1250$`

재화 획득 시 즉시 숫자를 갱신한다.

재화 이미지가 UI 위치까지 이동한 뒤 사라지는 연출을 추가할 수 있다.

---

## 3.4 수족관 플레이 영역

게임의 핵심 영역이다.

포함 대상:

- 구피
- 피라냐
- 사료
- 은화
- 금화
- 보석
- 진주
- 조개
- 청새치
- 포탈
- 에일리언
- 공격 이펙트

배경:

`Background_Stage.png`

수족관 영역을 클릭하면 사료 생성 요청으로 처리한다.

단, 클릭한 대상이 구매 버튼이나 재화, 에일리언인 경우 사료를 생성하면 안 된다.

---

## 3.5 좌측 하단 스테이지 UI

표시 예:

- `1-1`
- `1-2`
- `1-3`

스테이지가 변경될 때만 갱신한다.

---

## 3.6 중앙 하단 메시지 UI

상황별 안내 문구를 표시한다.

우선순위 예:

1. 에일리언 등장 경고
2. 아이템 해금 알림
3. 튜토리얼
4. 일반 안내

동시에 여러 메시지가 겹치지 않도록 한 번에 하나만 표시한다.

---

## 3.7 게임오버 오버레이

게임 화면 위에 반투명 검정 배경을 표시한다.

중앙 내용:

- 게임오버!
- 다시 하기 버튼

게임오버 중에는 게임 개체 이동과 클릭 처리를 중지한다.

---

## 3.8 부화장 화면

스테이지를 클리어했을 때 사용한다.

배경:

`Background_Hatchery.png`

1-1 완료:
- 알 부화
- 조개 획득 표시

1-2 완료:
- 알 부화
- 청새치 획득 표시

1-3 완료:
- 최종 엔딩 메시지

부화장 연출 동안에는 수족관 게임 로직을 일시 정지한다.

---

# 4. READY, PLAYING, GAME_OVER 게임 상태

게임 전체 상태는 세 가지로 제한한다.

---

## 4.1 READY

게임 실행 직후의 준비 상태이다.

역할:

- 이미지 준비
- 변수 초기화
- 기존 게임 개체 제거
- 기본 재화 설정
- 1-1 설정
- 구피 2마리 생성

준비가 완료되면 자동으로 `PLAYING`으로 변경한다.

사용자에게 별도의 시작 버튼은 보여주지 않는다.

---

## 4.2 PLAYING

실제 플레이 상태이다.

다음 기능이 작동한다.

- 구피 이동
- 허기 진행
- 사료 생성
- 사료 섭취
- 성장
- 재화 생성
- 재화 획득
- 구매
- 피라냐 이동
- 에일리언 등장
- 펫 행동
- 스테이지 진행

---

## 4.3 GAME_OVER

모든 구피 및 피라냐가 사망했을 때 진입한다.

처리:

- 게임 루프의 실제 진행 중지
- 허기 타이머 정지
- 에일리언 이동 정지
- 클릭 입력 차단
- 게임오버 오버레이 표시
- 다시 하기 버튼만 활성화

다시 하기 클릭:

`GAME_OVER → READY → PLAYING`

순서로 초기화한다.

---

## 4.4 보조 화면 상태

게임 전체 상태와 별도로 화면 종류를 저장하면 편하다.

`sceneMode` 예:

- `STAGE`
- `HATCHERY`
- `ENDING`

이 값은 `gameState`와 다른 역할이다.

예:

- 게임 정상 진행: `PLAYING + STAGE`
- 알 부화 연출: `PLAYING + HATCHERY`
- 최종 엔딩: `PLAYING + ENDING`

이렇게 구분하면 `READY / PLAYING / GAME_OVER` 구조를 유지하면서 화면 전환을 처리할 수 있다.

---

# 5. 저장해야 할 데이터

서버나 영구 저장은 필요하지 않다.

모든 데이터는 현재 브라우저 실행 중 JavaScript 메모리에만 저장한다.

---

## 5.1 전체 게임 데이터

| 데이터 | 예시 | 설명 |
|---|---|---|
| gameState | PLAYING | 현재 게임 상태 |
| sceneMode | STAGE | 현재 화면 |
| stage | 1 | 내부 스테이지 번호 |
| stageLabel | 1-1 | 화면 표시용 |
| money | 100 | 현재 보유 재화 |
| gameSpeed | 1 | 1배 / 4배 |
| feedLimit | 3 | 동시에 존재 가능한 사료 수 |
| eggProgress | 0 | 현재 알 구매 횟수 |
| alienActive | false | 에일리언 존재 여부 |
| alienTimer | 0 | 다음 에일리언 등장 시간 |
| warningActive | false | 경고 출력 여부 |
| clamUnlocked | false | 조개 획득 여부 |
| marlinUnlocked | false | 청새치 획득 여부 |

---

## 5.2 구피 데이터

구피마다 별도 데이터가 필요하다.

| 데이터 | 설명 |
|---|---|
| id | 개체 고유 번호 |
| x, y | 현재 위치 |
| targetX, targetY | 이동 목표 |
| direction | 왼쪽 / 오른쪽 |
| speed | 현재 이동 속도 |
| growthStage | 1 / 2 / 3 |
| eatenCount | 현재 성장 단계에서 먹은 사료 수 |
| hungerState | NORMAL / HUNGRY / STARVING / DEAD |
| hungerTimer | 허기 시간 |
| starvationTimer | 굶주림 시간 |
| coinTimer | 다음 재화 생성 시간 |
| alive | 생존 여부 |

---

## 5.3 피라냐 데이터

구피와 비슷하게 관리한다.

추가/차이점:

- 성장 단계 없음
- 사료 대신 1/3 구피를 먹음
- 보석 생성 타이머 보유

---

## 5.4 사료 데이터

| 데이터 | 설명 |
|---|---|
| id | 고유 번호 |
| x, y | 위치 |
| fallSpeed | 낙하 속도 |
| active | 존재 여부 |

---

## 5.5 재화 데이터

| 데이터 | 설명 |
|---|---|
| id | 고유 번호 |
| type | SILVER / GOLD / DIAMOND / PEARL |
| value | 획득 금액 |
| x, y | 위치 |
| fallSpeed | 낙하 속도 |
| collected | 획득 여부 |

진주는 낙하하지 않고 조개 위치에 고정한다.

---

## 5.6 에일리언 데이터

| 데이터 | 설명 |
|---|---|
| id | 고유 번호 |
| hp | 현재 체력 |
| maxHp | 30 |
| x, y | 위치 |
| targetId | 추적 중인 물고기 |
| direction | 현재 이동 방향 |
| speed | 이동 속도 |
| knockbackValue | 클릭에 의한 회피 누적 |
| alive | 생존 여부 |

---

## 5.7 조개 데이터

필요 데이터:

- 현재 열림 여부
- 진주 생성 타이머
- 입 열림 애니메이션 상태
- 진주 존재 여부
- 5초 제한 시간

---

## 5.8 청새치 데이터

필요 데이터:

- 위치
- 이동 목표
- 방향
- 공격 상태
- 에일리언 추적 여부
- 마지막 공격 시간

---

# 6. 사용자 입력과 처리 결과

## 6.1 수족관 빈 공간 클릭

조건:

- `gameState = PLAYING`
- `sceneMode = STAGE`
- 에일리언 없음
- 클릭 대상이 다른 버튼이나 개체가 아님
- 보유 금액 5$ 이상
- 현재 사료 수가 상한 미만

결과:

1. 재화 5$ 감소
2. 클릭 위치에 사료 생성
3. 재화 UI 갱신

조건을 만족하지 못하면 아무것도 생성하지 않는다.

---

## 6.2 재화 클릭

결과:

1. 해당 재화의 중복 클릭 방지
2. 획득 처리
3. 재화 값만큼 보유 금액 증가
4. 우측 상단 UI 방향으로 이동 연출
5. 재화 제거

---

## 6.3 구피 구매 슬롯 클릭

조건:

- 구피 슬롯 해금
- 현재 재화 100$ 이상
- 게임 진행 중

결과:

- 100$ 감소
- 1/3 구피 한 마리 생성

신규 구피의 허기 타이머는 새로 시작한다.

---

## 6.4 사료 상한 슬롯 클릭

조건:

- 슬롯 해금
- 현재 재화 100$ 이상
- 현재 상한 9 미만

결과:

- 100$ 감소
- 사료 상한 +1

최대값:

**9**

9에 도달하면 버튼을 비활성화한다.

---

## 6.5 피라냐 구매 슬롯 클릭

조건:

- 1-2 또는 1-3
- 피라냐 슬롯 해금
- 현재 재화 1000$ 이상

결과:

- 1000$ 감소
- 피라냐 생성

1-1에서는 성체 구피가 있어도 피라냐 구매는 불가능하다.

---

## 6.6 알 구매 슬롯 클릭

조건:

- 알 슬롯 해금
- 알 진행도 3 미만
- 스테이지별 필요 금액 이상 보유

결과:

1. 금액 감소
2. eggProgress +1
3. 알 이미지 프레임 변경
4. eggProgress가 3이면 스테이지 클리어 처리

---

## 6.7 에일리언 클릭

조건:

- 에일리언 활성 상태
- 클릭한 대상이 에일리언

결과:

1. HP 1 감소
2. 피격 이펙트 재생
3. 클릭 위치와 이동 방향 비교
4. 조건에 따라 작은 넉백 누적
5. HP가 0 이하이면 제거

에일리언을 클릭한 이벤트가 수족관 배경으로 전달되어 사료가 생성되지 않도록 이벤트 전달을 막아야 한다.

---

## 6.8 다시 하기 버튼

결과:

- 현재 오브젝트 전부 삭제
- 게임 변수 초기화
- READY 상태
- 1-1 초기 데이터 생성
- PLAYING 시작

---

## 6.9 스페이스바

선택 구현 기능이다.

권장 방식:

**누를 때마다 1배 ↔ 4배 전환**

이유:

누르고 있는 동안 처리하는 방식보다 모바일 확장과 키 입력 관리가 쉽다.

게임오버 또는 부화장에서는 배속을 변경하지 않는다.

---

## 6.10 모바일 터치

모바일에서는 마우스 클릭 대신 `pointer` 입력을 사용하면 마우스와 터치를 하나의 방식으로 처리할 수 있다.

처리 결과는 PC 클릭과 동일하게 한다.

---

# 7. 필요한 기능 목록과 각 기능의 역할

## 7.1 게임 초기화 기능

역할:

게임을 처음 시작하거나 다시 하기 때 모든 값을 초기 상태로 되돌린다.

초기값:

- stage = 1-1
- money = 100
- feedLimit = 3
- eggProgress = 0
- 구피 2마리
- 구매 슬롯 잠금
- 펫 없음

---

## 7.2 게임 루프 기능

역할:

게임이 진행되는 동안 지속적으로 각 개체의 상태를 갱신한다.

포함 기능:

- 이동
- 타이머
- 충돌
- 허기
- 재화 생성
- 에일리언 추적
- 화면 위치 반영

---

## 7.3 구피 생성 기능

역할:

초기 구피 또는 구매한 구피를 생성한다.

생성 시:

- 랜덤 위치
- 1/3 크기
- 기본 애니메이션
- 허기 타이머 초기화

---

## 7.4 랜덤 이동 기능

역할:

물고기가 수족관을 자연스럽게 돌아다니게 한다.

동작:

1. 임의 목표 좌표 선택
2. 목표 방향으로 이동
3. 목표 근처 도착
4. 잠시 대기하거나 새 목표 선택

---

## 7.5 사료 탐색 기능

역할:

구피가 사료가 존재할 때 가장 가까운 사료를 향해 빠르게 이동하게 한다.

사료가 사라지면 다시 랜덤 이동으로 복귀한다.

---

## 7.6 먹이 충돌 기능

역할:

구피와 사료, 피라냐와 작은 구피의 접촉을 검사한다.

구피 + 사료:

- 사료 제거
- 허기 초기화
- 성장 카운트 증가

피라냐 + 1/3 구피:

- 구피 제거
- 피라냐 허기 초기화
- 포식 이펙트

---

## 7.7 구피 성장 기능

역할:

먹은 사료 수에 따라 크기를 변경한다.

조건:

- 1/3 → 사료 3개 → 2/3
- 2/3 → 추가 사료 4개 → 3/3

성장 후:

- 화면 크기 변경
- 재화 생성 종류 변경
- 구매 슬롯 해금 검사

---

## 7.8 허기 시스템

역할:

플레이어가 계속 먹이를 공급하도록 만든다.

구피와 피라냐 기본 흐름:

`NORMAL → HUNGRY → STARVING → DEAD`

시간:

- NORMAL/HUNGRY 진입 기준: 랜덤 10~15초
- HUNGRY 유지: 5초
- STARVING 유지: 7초

굶주림 중 먹으면 NORMAL로 복귀한다.

에일리언이 존재하는 동안 허기 타이머는 증가하지 않는다.

---

## 7.9 사망 처리 기능

구피 또는 피라냐가 굶어 죽었을 때:

1. 상태를 DEAD로 변경
2. 다른 AI 처리 제외
3. 사망 이미지 사용
4. 뒤집힌 모습 표시
5. 천천히 아래로 이동
6. 바닥 도착 시 페이드
7. 목록에서 제거
8. 게임오버 검사

---

## 7.10 재화 생성 기능

구피:

- 2/3 → 10초마다 은화
- 3/3 → 10초마다 금화

피라냐:

- 일정 주기마다 보석

조개:

- 20초마다 진주 생성

드롭된 재화는 바닥 방향으로 이동한다.

진주는 조개 위치에 고정한다.

---

## 7.11 재화 수집 기능

역할:

플레이어가 클릭한 재화를 금액으로 변환한다.

중복 획득을 방지하기 위해 클릭 즉시 `collected` 상태로 변경한다.

---

## 7.12 구매 기능

공통 처리 순서:

1. 슬롯 잠금 확인
2. 현재 스테이지 조건 확인
3. 가격 확인
4. 재화 확인
5. 구매 처리
6. UI 갱신

구매 실패 시 값이 변하지 않아야 한다.

---

## 7.13 해금 검사 기능

구피 상태가 변할 때 검사한다.

구피 중 하나 이상이 2/3:

- 구피 슬롯 해금
- 사료 상한 슬롯 해금

구피 중 하나 이상이 3/3:

- 알 슬롯 해금
- 피라냐 슬롯 조건 충족

피라냐는 1-2 이상에서만 실제 활성화한다.

---

## 7.14 알 진행 기능

역할:

현재 스테이지의 목표를 관리한다.

알 구매 1회:

- 1/3 표시

2회:

- 2/3 표시

3회:

- 3/3 표시
- 스테이지 클리어

---

## 7.15 스테이지 전환 기능

1-1 완료:

- 부화장
- 조개 해금
- 1-2 시작

1-2 완료:

- 부화장
- 청새치 해금
- 1-3 시작

1-3 완료:

- 엔딩 화면

스테이지 전환 시 일반 물고기와 재화를 초기화하는 것을 권장한다.

이유:

이전 스테이지 개체를 모두 유지하면 밸런스와 상태 관리가 복잡해진다.

펫 해금 정보만 다음 스테이지로 유지한다.

---

## 7.16 에일리언 경고 기능

1-2와 1-3에서 사용한다.

다음 등장 시점 5초 전:

- 중앙 하단 경고 문구 표시
- 경고 효과음 재생

---

## 7.17 포탈 및 에일리언 생성

경고 종료 후:

1. 랜덤 위치 선택
2. 포탈 생성
3. 포탈 스프라이트 애니메이션
4. 에일리언 생성
5. 포탈 제거

---

## 7.18 에일리언 추적 기능

대상:

- 구피
- 피라냐

가장 가까운 살아있는 대상 하나를 선택해 이동한다.

접촉 시:

- 해당 물고기 즉시 제거
- 포식 이펙트
- 다음 대상 재탐색
- 게임오버 검사

---

## 7.19 에일리언 전투 기능

플레이어 클릭:

- 1 피해

청새치 접촉:

- 1초마다 1 피해

HP 0:

- 에일리언 제거
- 허기 시스템 재개
- 사료 생성 가능 상태 복귀

---

## 7.20 조개 기능

1-2와 1-3에서 등장한다.

주기:

- 20초 대기
- 입 열기
- 진주 생성
- 5초 유지
- 진주 미획득 시 삭제
- 입 닫기
- 다시 20초 대기

---

## 7.21 청새치 기능

1-3에서 등장한다.

평상시:

- 랜덤 이동

에일리언 존재 시:

- 공격 이미지
- 빠르게 에일리언 추적
- 접촉하면 1초마다 1 피해

에일리언 사망 후:

- 기본 이미지
- 랜덤 이동 복귀

---

## 7.22 게임오버 검사

다음 조건을 검사한다.

`살아있는 구피 수 + 살아있는 피라냐 수 = 0`

조건 만족:

- GAME_OVER 상태
- 오버레이 표시

조개와 청새치는 생존 판정에 포함하지 않는다.

---

# 8. 오류를 예방하기 위한 예외 상황

## 8.1 돈이 부족한 상태에서 사료 클릭

문제:
재화가 음수가 될 수 있다.

처리:
5$ 미만이면 사료를 생성하지 않는다.

---

## 8.2 사료 상한 초과

현재 사료 개수가 `feedLimit` 이상이면 추가 생성하지 않는다.

---

## 8.3 UI 클릭 시 사료 생성

구매 슬롯, 재화 UI, 메시지 영역 등을 클릭했는데 사료가 생성되면 안 된다.

대응:

- UI를 플레이 영역 밖에 배치
- 이벤트 전파 차단
- 클릭 대상 종류 확인

---

## 8.4 재화 더블 클릭

같은 동전을 빠르게 여러 번 눌러 중복 획득하는 것을 막아야 한다.

재화 클릭 직후:

`collected = true`

처리 후 추가 클릭 무시.

---

## 8.5 죽은 물고기가 다시 먹이를 먹는 문제

사망 상태에서는 다음 기능을 모두 제외한다.

- 이동 AI
- 사료 탐색
- 먹이 충돌
- 재화 생성
- 허기 갱신

---

## 8.6 동시에 여러 구피가 같은 사료를 먹는 문제

사료가 한 번 소비되면 즉시 비활성화 또는 배열에서 제거한다.

다른 구피는 이미 제거된 사료를 먹을 수 없다.

---

## 8.7 피라냐가 2/3 또는 성체 구피를 먹는 문제

피라냐의 먹이 대상은 반드시:

`growthStage === 1`

조건을 만족하는 구피만 선택한다.

---

## 8.8 에일리언이 죽은 물고기를 추적하는 문제

대상 탐색 시:

- alive = true
- death 상태가 아님

인 개체만 선택한다.

현재 목표가 사라지면 즉시 새 목표를 선택한다.

---

## 8.9 에일리언 중 사료 생성 문제

`alienActive === true`인 동안 배경 클릭 입력을 무시한다.

---

## 8.10 에일리언 중 허기 진행 문제

에일리언 등장 시 기존 허기 값을 초기화하지 않는다.

단순히 타이머 증가만 중지한다.

에일리언 사망 후 기존 값에서 이어서 진행한다.

---

## 8.11 알을 3회 이상 구매하는 문제

`eggProgress >= 3`이면 버튼을 즉시 잠그고 추가 입력을 받지 않는다.

스테이지 전환이 중복 호출되지 않도록 `stageClearProcessing` 같은 처리 중 상태를 둘 수 있다.

---

## 8.12 구매 버튼 연타

한 프레임에 여러 번 구매되어 돈이 비정상적으로 감소할 수 있다.

구매 처리 직전에 항상 현재 재화를 다시 검사한다.

---

## 8.13 사료 상한이 9를 넘는 문제

`feedLimit >= 9`이면 상한 구매 버튼을 비활성화한다.

---

## 8.14 게임오버 직전 구매

게임 상태가 `PLAYING`이 아닌 경우 모든 구매를 무시한다.

---

## 8.15 게임오버 후 기존 타이머 실행

다시 시작한 뒤 이전 게임의 타이머가 실행되면 에일리언 등이 중복 생성될 수 있다.

가능하면 개별 `setInterval` 사용을 줄이고 게임 루프 내부 타이머 변수로 관리한다.

별도 `setTimeout`을 사용했다면 재시작 때 반드시 제거한다.

---

## 8.16 스테이지 변경 후 이전 개체 잔존

새 스테이지 시작 전에 다음을 비운다.

- 구피
- 피라냐
- 사료
- 재화
- 에일리언
- 포탈
- 이펙트

그리고 새로운 기본 구피를 생성한다.

펫은 현재 스테이지의 해금 상태에 따라 다시 생성한다.

---

## 8.17 이미지 파일 로드 실패

이미지가 없는 경우 화면 전체가 멈추지 않도록 한다.

개발 중 브라우저 개발자 도구에서 404 오류가 없는지 확인한다.

특히 GitHub Pages는 파일명의 대소문자를 구분할 수 있으므로 실제 파일명과 코드의 경로를 정확히 일치시킨다.

예:

`Guppy_Default.png`

와

`guppy_default.png`

는 다른 파일로 취급될 수 있다.

---

## 8.18 탭이 비활성화된 경우

브라우저 탭을 오래 벗어났다가 돌아왔을 때 한 번에 매우 큰 시간이 적용되면 모든 물고기가 즉시 굶어 죽을 수 있다.

프레임 시간 차이의 최대값을 제한하는 방식으로 예방한다.

---

## 8.19 부화장 중 입력 문제

부화장에서는:

- 사료 생성 금지
- 구매 금지
- 에일리언 공격 금지
- 재화 클릭 금지

화면 연출만 진행한다.

---

## 8.20 진주 중복 생성

조개에 이미 진주가 존재하면 새로운 진주를 생성하지 않는다.

---

# 9. PC와 모바일 반응형 조건

게임의 기본 대상은 PC지만 모바일 브라우저에서도 화면이 깨지지 않도록 구성한다.

---

## 9.1 공통 비율

게임 화면:

**16:9 비율 유지**

최대 너비를 설정하고 화면이 작아지면 비율에 맞춰 함께 축소한다.

---

## 9.2 PC

권장 조건:

- 1024px 이상
- 구매 슬롯 상단 가로 배치
- 재화 UI 우측 상단
- 스테이지 UI 좌측 하단
- 안내 메시지 중앙 하단

마우스 커서로 작은 재화를 쉽게 클릭할 수 있도록 재화 클릭 영역은 이미지보다 약간 넓게 잡는다.

---

## 9.3 태블릿

768px 전후에서는:

- 전체 UI를 일정 비율로 축소
- 상단 구매 슬롯은 한 줄 유지
- 텍스트 크기를 너무 작게 줄이지 않음

---

## 9.4 모바일

권장 최소 너비:

320px

처리 원칙:

- 게임 화면 전체를 화면 너비에 맞춤
- 구매 버튼 4개가 한 줄에 보이도록 아이콘 크기 축소
- 가격 텍스트는 읽을 수 있는 최소 크기 유지
- 재화 숫자는 우측 상단 고정
- 안내 메시지는 화면 너비의 약 80~90% 사용

---

## 9.5 터치 입력

모바일에서는 손가락으로 작은 동전이나 에일리언을 누르기 어렵다.

따라서 실제 이미지 크기보다 클릭 가능한 영역을 약간 크게 설정한다.

특히:

- 은화
- 금화
- 보석
- 에일리언

의 터치 영역을 충분히 확보한다.

---

## 9.6 모바일 배속

모바일에는 스페이스바가 없으므로 4배속 기능을 반드시 지원해야 한다면 작은 `x1 / x4` 버튼을 추가할 수 있다.

단, 이 기능은 MVP 이후 구현한다.

---

## 9.7 세로 화면

모바일 세로 화면에서도 실행은 가능하게 하되, 게임 특성상 가로 화면이 더 적합하다.

선택적으로 다음 메시지를 표시할 수 있다.

> 가로 화면에서 플레이하면 더 편합니다.

화면 회전을 강제로 막지는 않는다.

---

# 10. 기능 구현 순서

제작 시간이 짧기 때문에 아래 순서를 변경하지 않는 것을 권장한다.

---

## 1단계. 정적 화면 만들기

구현:

- 배경
- 상단 구매 슬롯
- 재화 UI
- 스테이지 UI
- 메시지 UI

이 단계에서는 게임 기능을 넣지 않는다.

목표:

레이아웃이 먼저 정상적으로 보이게 한다.

---

## 2단계. 기본 게임 상태 만들기

구현:

- READY
- PLAYING
- GAME_OVER
- money
- stage
- feedLimit
- eggProgress

목표:

게임에서 사용할 핵심 값이 한곳에서 관리되도록 한다.

---

## 3단계. 구피 생성과 이동

구현:

- 구피 2마리 생성
- 랜덤 목표 위치
- 좌우 이동
- 화면 경계 제한
- 스프라이트 애니메이션

목표:

화면 안에서 물고기가 계속 자연스럽게 움직인다.

---

## 4단계. 사료 시스템

구현:

- 배경 클릭
- 5$ 차감
- 사료 생성
- 낙하
- 상한 3 적용

다음으로:

- 구피 사료 탐색
- 사료 접촉
- 사료 제거

---

## 5단계. 성장 및 허기

구현:

- 먹은 사료 수 저장
- 1/3 → 2/3
- 2/3 → 3/3
- 허기
- 굶주림
- 사망

목표:

구피 육성의 기본 사이클 완성.

---

## 6단계. 재화 시스템

구현:

- 은화 생성
- 금화 생성
- 재화 낙하
- 클릭 수집
- 금액 증가

목표:

`먹이 → 성장 → 돈 생성 → 돈 획득`

게임의 핵심 경제 루프를 완성한다.

---

## 7단계. 구매 및 해금

구현:

- 구피 구매
- 사료 상한 증가
- 성장을 통한 슬롯 해금
- 최대 사료 상한 9

다음:

- 피라냐
- 알

---

## 8단계. 알과 스테이지 진행

구현:

- Egg.png 3개 프레임
- 알 구매
- 스테이지별 가격
- 알 3회 구매
- 부화장 화면
- 1-1 → 1-2
- 1-2 → 1-3
- 1-3 → 엔딩

이 단계까지 완료하면 게임을 처음부터 끝까지 진행할 수 있는 기본 구조가 완성된다.

---

## 9단계. 피라냐

구현:

- 1-2부터 구매 가능
- 랜덤 이동
- 1/3 구피 추적
- 포식
- 굶주림
- 보석 생성

---

## 10단계. 게임오버

구현:

- 구피 + 피라냐 생존 수 검사
- GAME_OVER 전환
- 오버레이
- 다시 하기

---

## 11단계. 에일리언

구현 순서:

1. 1-2/1-3 여부 검사
2. 60초 타이머
3. 5초 경고
4. 포탈
5. 에일리언 생성
6. 물고기 추적
7. 포식
8. 클릭 공격
9. HP 30
10. 사망

이후 시간이 남으면 넉백 방향 전환을 추가한다.

---

## 12단계. 펫

먼저 조개:

- 고정 배치
- 20초 타이머
- 개폐 애니메이션
- 진주

다음 청새치:

- 랜덤 이동
- 에일리언 추적
- 1초마다 공격

---

## 13단계. 부가 기능

시간이 남을 때만 구현:

- 4배속
- 에일리언 넉백
- 재화 UI 이동 애니메이션
- 효과음
- 성장 이펙트
- 해금 이펙트
- 모바일 배속 버튼

---

# 11. 기능별 완료 조건

각 기능은 아래 조건을 만족해야 완료된 것으로 판단한다.

---

## 11.1 화면 UI

- [ ] 푸른 수족관 배경이 표시된다.
- [ ] 상단에 4개의 구매 슬롯이 있다.
- [ ] 우측 상단에 현재 재화가 표시된다.
- [ ] 좌측 하단에 스테이지가 표시된다.
- [ ] 중앙 하단에 메시지를 표시할 수 있다.
- [ ] 화면 크기를 줄여도 UI가 심하게 겹치지 않는다.

---

## 11.2 READY 상태

- [ ] 새 게임 시작 시 모든 기존 개체가 제거된다.
- [ ] 재화가 100$로 설정된다.
- [ ] 스테이지가 1-1로 설정된다.
- [ ] 구피 2마리가 생성된다.
- [ ] 두 구피가 초기 허기 진행 상태를 가진다.
- [ ] 사료 상한이 3이다.
- [ ] 구매 슬롯이 잠겨 있다.
- [ ] 초기화 완료 후 PLAYING으로 전환된다.

---

## 11.3 구피 이동

- [ ] 구피가 화면 안에서만 이동한다.
- [ ] 일정 시간 후 새로운 목표 위치를 선택한다.
- [ ] 왼쪽 이동 시 이미지 방향이 자연스럽다.
- [ ] 오른쪽 이동 시 이미지 방향이 자연스럽다.
- [ ] 두 마리 이상이 서로 독립적으로 움직인다.

---

## 11.4 사료 생성

- [ ] 빈 수족관 영역 클릭 시 사료가 생성된다.
- [ ] 생성 시 5$가 차감된다.
- [ ] 5$ 미만이면 생성되지 않는다.
- [ ] 사료는 아래로 천천히 이동한다.
- [ ] 사료 수가 상한 이상이면 생성되지 않는다.
- [ ] 에일리언 존재 중에는 생성되지 않는다.

---

## 11.5 사료 섭취

- [ ] 사료가 있으면 구피가 사료 방향으로 이동한다.
- [ ] 접촉하면 사료가 한 번만 삭제된다.
- [ ] 구피 허기가 초기화된다.
- [ ] 성장용 먹이 카운트가 증가한다.

---

## 11.6 구피 성장

- [ ] 1/3 구피가 사료 3개 섭취 후 2/3이 된다.
- [ ] 2/3 구피가 추가 사료 4개 섭취 후 3/3이 된다.
- [ ] 성장 단계에 따라 화면 크기가 달라진다.
- [ ] 2/3 등장 시 구피/사료 상한 구매가 해금된다.
- [ ] 3/3 등장 시 알이 해금된다.
- [ ] 1-2 이상이면 피라냐도 해금된다.

---

## 11.7 허기와 사망

- [ ] 구피가 일정 시간 후 허기 상태가 된다.
- [ ] 이후 굶주림 상태 이미지로 변경된다.
- [ ] 굶주림 상태에서 제한 시간 경과 시 죽는다.
- [ ] 사망 후 자율 이동하지 않는다.
- [ ] 사망 이미지가 표시된다.
- [ ] 아래로 가라앉는다.
- [ ] 바닥에서 사라진다.
- [ ] 에일리언 존재 중에는 허기 타이머가 멈춘다.

---

## 11.8 재화 생성

- [ ] 2/3 구피가 은화를 생성한다.
- [ ] 3/3 구피가 금화를 생성한다.
- [ ] 재화가 천천히 아래로 떨어진다.
- [ ] 바닥까지 미획득 시 사라진다.

---

## 11.9 재화 획득

- [ ] 은화 클릭 시 15$ 증가한다.
- [ ] 금화 클릭 시 25$ 증가한다.
- [ ] 보석 클릭 시 200$ 증가한다.
- [ ] 진주 클릭 시 350$ 증가한다.
- [ ] 같은 재화를 두 번 획득할 수 없다.
- [ ] 우측 상단 금액이 즉시 갱신된다.

---

## 11.10 구피 구매

- [ ] 슬롯 해금 전에는 클릭할 수 없다.
- [ ] 100$ 이상일 때 구매된다.
- [ ] 100$가 차감된다.
- [ ] 새 1/3 구피가 생성된다.
- [ ] 돈이 부족하면 아무 변화도 없다.

---

## 11.11 사료 상한 구매

- [ ] 100$를 사용해 상한이 1 증가한다.
- [ ] 최대값은 9이다.
- [ ] 9에서는 더 구매할 수 없다.

---

## 11.12 피라냐

- [ ] 1-1에서는 구매할 수 없다.
- [ ] 1-2 이상에서 조건 충족 시 구매할 수 있다.
- [ ] 가격은 1000$이다.
- [ ] 1/3 구피만 먹는다.
- [ ] 2/3 및 3/3 구피는 먹지 않는다.
- [ ] 먹이를 먹으면 허기가 초기화된다.
- [ ] 일정 시간마다 보석을 생성한다.

---

## 11.13 알

- [ ] 알은 조건 만족 후 해금된다.
- [ ] 각 스테이지 가격이 정확하다.
- [ ] 구매할 때마다 1/3 → 2/3 → 3/3으로 변경된다.
- [ ] 3회 구매 후 추가 구매할 수 없다.
- [ ] 3회 구매 시 스테이지 클리어 처리가 한 번만 실행된다.

---

## 11.14 스테이지 전환

- [ ] 1-1 완료 후 부화장 화면이 나온다.
- [ ] 조개가 해금된다.
- [ ] 1-2 시작 시 조개가 자동 등장한다.
- [ ] 1-2 완료 후 청새치가 해금된다.
- [ ] 1-3 시작 시 조개와 청새치가 자동 등장한다.
- [ ] 1-3 완료 후 엔딩 화면이 나온다.

---

## 11.15 조개

- [ ] 1-2부터 바닥에 표시된다.
- [ ] 약 20초마다 입을 연다.
- [ ] 입이 열린 상태에서 진주가 생성된다.
- [ ] 진주는 조개 위치에 고정된다.
- [ ] 5초 안에 클릭하지 않으면 진주가 사라진다.
- [ ] 이후 조개가 다시 닫힌다.

---

## 11.16 에일리언 경고

- [ ] 1-2와 1-3에서만 작동한다.
- [ ] 등장 약 5초 전에 경고 메시지가 표시된다.
- [ ] 경고 후 포탈이 표시된다.
- [ ] 포탈 이후 에일리언이 생성된다.

---

## 11.17 에일리언

- [ ] 초기 HP는 30이다.
- [ ] 살아있는 구피 또는 피라냐를 추적한다.
- [ ] 접촉한 물고기를 즉시 제거한다.
- [ ] 에일리언 클릭 시 HP가 1 감소한다.
- [ ] 클릭 시 피격 이펙트가 표시된다.
- [ ] HP 0 이하에서 제거된다.
- [ ] 제거 후 허기 시스템이 다시 진행된다.
- [ ] 제거 후 사료를 다시 생성할 수 있다.

---

## 11.18 청새치

- [ ] 1-3에서 자동으로 등장한다.
- [ ] 평소에는 랜덤 이동한다.
- [ ] 에일리언 등장 시 에일리언을 추적한다.
- [ ] 접촉 중 1초마다 1 피해를 준다.
- [ ] 에일리언 사망 후 평상시 상태로 돌아간다.

---

## 11.19 게임오버

- [ ] 구피와 피라냐가 모두 사라지면 GAME_OVER가 된다.
- [ ] 조개와 청새치는 생존 판정에서 제외된다.
- [ ] 게임오버 오버레이가 표시된다.
- [ ] 게임 진행이 멈춘다.
- [ ] 다시 하기 버튼이 표시된다.
- [ ] 다시 하기 후 1-1부터 정상 시작된다.
- [ ] 이전 게임의 에일리언이나 타이머가 남지 않는다.

---

## 11.20 반응형

- [ ] PC에서 16:9 화면이 정상적으로 표시된다.
- [ ] 작은 브라우저에서도 게임 영역이 잘리지 않는다.
- [ ] 모바일에서도 구매 UI가 화면 밖으로 나가지 않는다.
- [ ] 모바일에서 터치로 사료 생성과 재화 획득이 가능하다.
- [ ] 작은 아이템의 터치 영역이 충분하다.

---

# 12. 스테이지별 설정 요약

| 항목 | 1-1 | 1-2 | 1-3 |
|---|---|---|---|
| 시작 재화 | 100$ | 스테이지 시작 기준값 사용 | 스테이지 시작 기준값 사용 |
| 구피 | O | O | O |
| 피라냐 | X | O | O |
| 에일리언 | X | O | O |
| 조개 | X | O | O |
| 청새치 | X | X | O |
| 알 가격 | 750$ | 1500$ | 2500$ |
| 클리어 보상 | 조개 | 청새치 | 엔딩 |

스테이지 변경 시 경제 밸런스가 너무 어려우면 개발 테스트 단계에서 시작 재화를 별도로 조정할 수 있다.

단, 최종 게임 규칙을 변경할 경우 `config.js`의 설정값만 수정할 수 있도록 설계한다.

---

# 13. 이미지 스프라이트 처리 규칙

## 13.1 3x3 스프라이트

대상:

- Alien.png
- Alien_Portal.png
- Clam_Default.png
- Clam_Open.png
- Effect_Carnivore.png
- Effect_Hit.png
- Guppy_Default.png
- Guppy_Starvation.png
- Piranha_Default.png
- Piranha_Starvation.png
- StripedMarlin_Anger.png
- StripedMarlinDefault.png

총 9프레임이다.

기본 반복 순서:

`0 → 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 → 반복`

---

## 13.2 Clam_Open.png

프레임 의미:

| 프레임 | 동작 |
|---:|---|
| 0 | 닫힌 상태 시작 |
| 1 | 조금 열림 |
| 2 | 열리는 중 |
| 3 | 거의 열림 |
| 4 | 완전히 열림 |
| 5 | 닫기 시작 |
| 6 | 닫는 중 |
| 7 | 거의 닫힘 |
| 8 | 닫힌 상태 |

4번 프레임에서는 5초 동안 정지하거나 반복 없이 유지한다.

---

## 13.3 Egg.png

3x1 스프라이트.

| 프레임 | 상태 |
|---:|---|
| 0 | 1/3 |
| 1 | 2/3 |
| 2 | 3/3 |

---

## 13.4 사망 이미지

- Guppy_Dead.png
- Piranha_Dead.png

단일 이미지이므로 애니메이션 없이 사용한다.

CSS 회전 또는 이미지 자체 방향을 이용해 뒤집혀 가라앉는 모습으로 표현한다.

---

# 14. 초보자용 구현 원칙

## 14.1 한 번에 한 기능만 구현한다

예를 들어 구피를 만들 때 처음부터 허기, 성장, 돈 생성까지 한 번에 만들지 않는다.

순서:

1. 화면에 표시
2. 움직이기
3. 사료 찾기
4. 사료 먹기
5. 성장
6. 허기
7. 돈 생성

각 단계가 정상 작동하는 것을 확인하고 다음 단계로 넘어간다.

---

## 14.2 게임 규칙 숫자는 한곳에 모은다

가격이나 시간 값을 코드 곳곳에 직접 작성하지 않는다.

한곳에서 관리할 대상:

- 가격
- 이동 속도
- 허기 시간
- 재화 가치
- 에일리언 HP
- 성장 필요량

이렇게 하면 밸런스를 수정할 때 실수를 줄일 수 있다.

---

## 14.3 배열에서 게임 개체를 관리한다

각 종류별로 목록을 만든다.

관리 대상:

- 구피 목록
- 피라냐 목록
- 사료 목록
- 재화 목록
- 에일리언 목록 또는 단일 에일리언
- 이펙트 목록

개체를 삭제할 때 화면 요소와 데이터 양쪽을 함께 정리한다.

---

## 14.4 상태를 문자열 또는 정해진 값으로 관리한다

허기 상태를 여러 개의 불리언으로 나누지 않는다.

좋은 구조:

- NORMAL
- HUNGRY
- STARVING
- DEAD

이렇게 하면 동시에 `hungry=true`, `dead=true` 같은 잘못된 상태가 생기는 것을 줄일 수 있다.

---

# 15. 최종 개발 완료 체크리스트

## 게임 흐름

- [ ] 페이지 접속 즉시 1-1이 시작된다.
- [ ] 기본 재화가 100$다.
- [ ] 허기 상태의 1/3 구피 2마리가 시작한다.
- [ ] 튜토리얼 메시지가 표시된다.
- [ ] 구피를 성장시킬 수 있다.
- [ ] 재화를 모을 수 있다.
- [ ] 구매 슬롯이 성장 조건에 맞춰 해금된다.
- [ ] 알을 완성해 1-2로 이동할 수 있다.
- [ ] 1-2에서 에일리언이 등장한다.
- [ ] 1-1 보상 조개가 1-2에서 등장한다.
- [ ] 1-2를 완료해 1-3으로 이동할 수 있다.
- [ ] 1-2 보상 청새치가 1-3에서 등장한다.
- [ ] 1-3을 완료하면 엔딩이 나온다.

## 실패 처리

- [ ] 굶주린 물고기가 죽는다.
- [ ] 에일리언이 물고기를 잡아먹을 수 있다.
- [ ] 모든 구피와 피라냐가 없어지면 게임오버가 된다.
- [ ] 다시 하기 기능이 정상 작동한다.

## 기술 조건

- [ ] HTML, CSS, 순수 JavaScript만 사용했다.
- [ ] 외부 라이브러리가 없다.
- [ ] 서버 코드가 없다.
- [ ] 모든 이미지 경로가 상대 경로다.
- [ ] GitHub Pages에서 실행할 수 있다.
- [ ] 개발자 도구 Console에 치명적인 오류가 없다.
- [ ] 이미지 404 오류가 없다.
- [ ] 새로고침 후 게임을 다시 정상 시작할 수 있다.

---

# 16. 최종 구현 목표

이 프로젝트에서 가장 중요한 목표는 화려한 연출이 아니라 다음 플레이 흐름이 끊기지 않는 것이다.

**게임 시작 → 사료 공급 → 구피 성장 → 재화 획득 → 구매 → 알 완성 → 다음 스테이지 → 에일리언 대응 → 펫 획득 → 1-3 엔딩**

위 흐름을 한 번이라도 처음부터 끝까지 오류 없이 플레이할 수 있다면 기본 프로젝트는 완료된 것으로 판단한다.

이후 남는 시간에 다음 요소를 추가한다.

- 효과음
- 4배속
- 부드러운 재화 이동
- 에일리언 넉백
- 성장 이펙트
- 해금 이펙트
- 부화 연출 개선

**기능의 수보다 완주 가능한 게임 한 판을 먼저 완성하는 것을 최우선으로 한다.**
