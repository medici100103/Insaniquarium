# Aqua Guppy · 구피의 작은 바다 — 개발 설계서

기준일: 2026-09-12. 현재 소스의 구조·상태·수치·입력·표현을 기록한다. 플레이 규칙은 [game-plan.md](game-plan.md), 실행 안내는 [README.md](README.md)를 함께 참고한다.

## 1. 실행 구조

외부 라이브러리 없이 HTML 요소와 JavaScript 객체로 구성한 정적 웹게임이다. `index.html`을 직접 열 수 있도록 상대 경로와 일반 `defer` 스크립트를 사용한다. 게임 실행에는 서버·Node.js·빌드가 필요하지 않다. 데이터는 메모리에만 존재하며 저장·불러오기 기능은 없다.

| 경로 | 현재 역할 |
| --- | --- |
| `index.html` | 수족관, 상점, 지갑, 목표, 가이드, 부화장, 결과·일시정지 오버레이 |
| `css/style.css` | 배치, 반응형, 스프라이트, 잠금·잔액 부족, 입수·성장·재화 이펙트 |
| `js/config.js` | `Aqua.CONFIG`: 가격, 시간, 크기, 이동·전투, 배속, 음원 매핑 |
| `js/entities.js` | 물고기 팩토리, 난수, 거리, 목표 위치, 이동·경계 보조 함수 |
| `js/game.js` | DOM과 독립적인 `Aqua.Game`, 개체·경제·전투·스테이지 상태와 이벤트 |
| `js/audio.js` | `Aqua.AudioManager`, 배경음 전환·효과음 풀·음소거·재생 제한 처리 |
| `js/input.js` | `Aqua.SpeedHoldInput`, 키보드·포인터 홀드 및 해제 처리 |
| `js/main.js` | 이미지 로딩, 게임 루프, DOM 렌더링, 일반 입력, 이벤트와 음원 연결 |
| `Images/` | 제공된 PNG 24개 |
| `Sounds/` | MP3 9개 |
| `tests/game.test.cjs` | 게임 규칙과 정상 조작 기반 캠페인 시뮬레이션 |
| `tests/audio.test.cjs` | 볼륨·파일 매핑·배경음·효과음·음소거·재생 거부 처리 |
| `tests/input.test.cjs` | 홀드·반복 키·포인터 취소·포커스 이탈·입력 중첩 검증 |
| `tests/preview.cjs` | 개발 확인용 선택적 localhost 정적 서버 |

스크립트 순서는 config → entities → game → audio → input → main이다. `globalThis.Aqua` 네임스페이스를 공유하며, 모델과 오디오·입력 클래스를 브라우저 DOM 없이도 테스트할 수 있다.

## 2. 좌표와 게임 루프

### 좌표·크기

기준 영역은 1280×720, 원점은 수족관 좌측 상단이다. X는 오른쪽, Y는 아래로 증가한다. DOM은 게임 좌표를 CSS `--world-scale`로 축소하고 `ResizeObserver`로 화면 너비에 맞춘다. 입력 좌표도 수족관의 실제 사각형에서 기준 좌표로 변환한다.

| 항목 | 값 |
| --- | --- |
| 물고기 이동 경계 | X 65~1215, Y 185~598 |
| 사료·재화·사망 물고기 바닥 | Y 624 |
| 구피 성장 크기 | 76, 106, 140 정사각형 |
| 피라냐 / 조개 | 135 / 132 정사각형 |
| 청새치 / 에일리언 / 포탈 | 195×175 / 226.2×223.6 / 205×205 |
| 은화·금화·보석 | 78.75×78.75 |
| 진주 | 99×99 |
| 사료 | 27×27 |

재화의 버튼 영역은 `max(이미지 크기 + 24, 32 / 화면 배율)`이며 패딩으로 이미지를 중앙에 배치한다. 작은 화면에서도 터치 영역을 확보한다.

### 시간 처리

`requestAnimationFrame` 하나에서 갱신한다. 실제 프레임 간격은 최대 0.1초로 제한한다. 일반 플레이는 `dt × game.speed`를 누적한 뒤 **0.025초 고정 간격**으로 `Game.update`를 호출한다. 모델은 이미 배속이 반영된 시간을 받는다.

부화장도 `dt × game.speed`로 진행한다. 입력 배속 변경·일시정지·장면 전환 때 누적 시간을 정리해 이전 배율의 잔여 시간이 섞이지 않도록 한다. 배속은 1 또는 `CONFIG.developerSpeed = 3`이다.

허기·유영·사료 낙하·재화 생산 및 이동·에일리언·펫·입수·부화는 게임 시간 기준이다. 안내 메시지 수명, 장식용 CSS 효과와 MP3 재생 속도는 실제 시간 기준이다. 일시정지 중에도 장식 UI는 움직일 수 있다.

## 3. 게임 상태와 데이터

### 상태

| 구분 | 값과 의미 |
| --- | --- |
| 로딩 | `main.js`의 `ready = false`; 별도 모델 READY 상태는 없음 |
| 진행 상태 | `game.state = PLAYING / GAME_OVER` |
| 장면 | `game.scene = STAGE / HATCHERY / ENDING` |
| 일시정지 | `game.paused` |
| 일반 플레이 가능 | PLAYING + STAGE + 일시정지 아님 |
| 전투 중 | `game.threat`: 포탈 또는 에일리언이 존재 |
| 배속 | `game.speed`: 현재 홀드 입력에 따른 1 또는 3 |

이미지 24개의 로딩이 모두 성공하면 새 게임을 시작한다. 실패하면 해당 이미지 경로를 로딩 화면에 표시하고 시작하지 않는다. 탭이 숨겨진 상태라면 시작 직후 일시정지한다.

`startStage`는 돈·개체·해금·알·전투·펫·스테이지 시간을 초기화한다. `restart`는 누적 기록도 초기화하고 1-1을 시작한다. 두 함수 모두 홀드 입력으로 설정된 배속을 바꾸지 않는다.

### 데이터

| 대상 | 주요 데이터 |
| --- | --- |
| Game | stage, state, scene, paused, speed, time, money, capacity, egg, unlocks, fish, food, coins, portal, alien, clam, marlin, hatchTime |
| 누적 기록 | collected, fed, aliens, elapsed |
| 물고기 | id, type, x/y, target, direction, speed, growth, meals, alive, removed, hunger, hungerThreshold, hungerState, coinTimer, mealTimer, idle, deathTime, phase, arrival |
| 입수 | phase(FALLING/SETTLING), velocity, time |
| 사료 | id, x/y, floorTime, removed |
| 재화 | id, type, value, x/y, collected, removed, floorTime, flyTime, 수집 시작 위치 |
| 에일리언 | id, x/y, hp, direction, stun, knocks, retreat |
| 조개 | id, x/y, phase, timer, pearlId |
| 청새치 | id, x/y, target, direction, attackTimer |

살아 있는 물고기 목록 `living`에는 낙하 중인 구피도 포함한다. `underwater`는 그중 FALLING 상태만 제외한다. 사망·제거된 개체는 먹이 및 적의 목표에서 제외한다.

모델이 `emit`한 이벤트를 `main.js`가 `drainEvents`로 한 번씩 소비해 UI·효과·소리를 반영한다. 주요 이벤트는 stage, notice, feed, eat, grow, unlock, purchase, splash, collect, death, chomp, warning, portal, alien, hit, defeat, pearl, clear, hatch, ending, gameover, pause, speed다.

## 4. 입력 설계

### 일반 입력

수족관 `pointerdown`을 게임 좌표로 바꿔 사료를 생성한다. 버튼·상점·지갑·카운터·목표·경고·하단 UI·오버레이 클릭은 사료 입력에서 제외한다.

재화와 에일리언은 `pointerdown`에서 처리하고 전파를 막는다. 키보드로 활성화된 `click`만 별도로 처리하여 한 번의 포인터 입력이 중복 획득·공격으로 이어지지 않는다. 구매는 모델이 잠금·가격·활성 상태를 다시 검사한다.

P 또는 일시정지 버튼은 STAGE의 PLAYING 상태에서만 동작한다. 가이드는 열기 전 진행 상태를 기억하고 닫을 때 필요한 경우에만 재개한다. 탭 비활성화 시 자동 일시정지하며 복귀 시 사용자가 직접 재개한다.

### 개발용 3배속 홀드

`SpeedHoldInput`은 스페이스바, 배속 버튼의 포인터, 해당 버튼의 Enter 입력을 독립된 Set 항목으로 관리한다.

1. 스페이스바 `keydown`: 기본 스크롤·버튼 활성화를 막고 홀드 시작. 자동 반복은 무시한다.
2. 스페이스바 `keyup`: 문서의 capture 단계에서 홀드를 해제한다. 포커스가 다른 요소로 이동해도 처리한다.
3. 배속 버튼 `pointerdown`: 왼쪽 마우스·터치만 허용하고 포인터를 캡처한다.
4. 창의 `pointerup`, `pointercancel`, 버튼의 `lostpointercapture`: 해당 포인터 홀드를 해제한다.
5. 버튼에 포커스가 있을 때 Enter는 누름·해제에 맞춰 같은 방식으로 처리한다.
6. `window.blur` 또는 숨겨진 탭은 모든 홀드를 비운다. 키 해제 이벤트를 놓쳐도 3배속이 남지 않는다.

하나라도 눌려 있으면 `Game.setSpeedHeld(true)`가 3으로 설정하고, 모두 해제되면 false가 1로 설정한다. 같은 상태를 반복 전달해도 배속이 뒤집히거나 speed 이벤트가 중복 발생하지 않는다. 배속 버튼의 일반 click에는 전환 동작이 없다.

입력은 이미지 로딩 후 활성화하며, 전투·부화장·가이드·일시정지·게임오버·엔딩에서도 유지·해제를 처리한다. 게임 이벤트가 홀드를 취소하지 않는다. 정지 중에는 배속 표시만 바뀌며 시뮬레이션을 재개하지 않는다. UI는 현재 1×/3×와 홀드 안내, 활성 스타일 및 `aria-pressed`를 표시한다.

## 5. 물고기와 사료

### 허기·성장·이동

`needsFood`는 alive, 입수 중이 아님, HUNGRY 또는 STARVING을 모두 요구한다. NORMAL 상태는 사료를 추적·소비하지 않는다.

| 설정 | 값 |
| --- | --- |
| 정상에서 허기까지 | 식사마다 무작위 10~15초 |
| 허기 유지 / 이후 굶주림 | 5초 / 7초 후 사망 |
| 성장 필요 식사 | 1→2단계 3회, 2→3단계 추가 4회 |
| 식사 쿨다운 | 1.1초 |
| 일반 유영 속도 | 24~52 좌표/초 |
| 먹이 추적 속도 | 165 좌표/초 |
| 구피 사료 섭취 거리 | 22 미만 |
| 피라냐 구피 섭취 거리 | 31 미만 |
| 사료 낙하 속도 | 25 좌표/초 |

구피는 가까운 유효 사료를, 피라냐는 수중의 살아 있는 1/3 구피를 찾는다. 먹이를 찾지 않을 때는 무작위 목표를 향해 이동하고 잠시 머무른다. 포탈·전투 중에는 허기 진행과 먹이 추적·섭취를 멈춘다.

식사는 허기를 0으로 만들고 다음 임계 시간을 다시 정한다. 성장 때 해당 단계의 식사 횟수를 0으로 초기화한다. 한 사료는 즉시 removed 처리하고 다음 물고기의 후보에서 제외한다. 업데이트는 더 배고픈 물고기를 우선한다.

### 구피 입수

구매는 `dropGuppy`로 새 구피를 만든다. 시작 구피·피라냐 생성은 일반 `addFish`를 사용한다.

| 설정 | 값 |
| --- | --- |
| 시작 X | 150~1130 중 무작위 |
| 시작 Y | -50 |
| 초기 아래 방향 속도 | 130 좌표/초 |
| 중력 가속도 | 1100 좌표/초² |
| 수면 Y | 185 |
| 잠기는 깊이 | 55 |
| 수면 아래 정착 시간 | 0.4초 |

FALLING에서는 속도에 중력을 누적하고 수면 도달 시 SETTLING으로 바꿔 splash를 한 번 발생시킨다. SETTLING은 3차 ease-out으로 깊이 55까지 내려간 뒤 arrival을 null로 지운다. 완료 전에는 허기·섭취·생산·일반 AI를 진행하지 않는다.

입수 개체는 별도 `#arrivals` 레이어에 두어 상단 UI 앞을 지나 내려오게 한다. 낙하 기울기는 -65도, 정착 중 0도로 돌아오며 완료 후 일반 개체 레이어로 옮긴다. 물보라 고리와 물방울 10개, FishDrop 효과음을 수면 도달 이벤트에 연결한다.

### 사망

굶주림 사망은 DEAD 이미지로 교체한 뒤 초당 45씩 바닥으로 이동하고, 바닥에서 2초에 걸쳐 사라진다. 사망 PNG에 상하반전이 포함되어 있으므로 추가 회전·상하반전을 하지 않는다. 포식 사망은 removed를 즉시 지정하고 chomp 이벤트를 발생시킨다.

## 6. 경제·해금·재화

가격은 사료 $5, 구피 $100, 사료 상한 +1 $100, 피라냐 $1,000이다. 사료 상한은 초기 3, 최대 9다.

| 생산자 | 주기 | 재화 | 가치 |
| --- | --- | --- | ---: |
| 2/3 구피 | 10초 | 은화 | $15 |
| 성체 구피 | 10초 | 금화 | $25 |
| 피라냐 | 15초 | 보석 | $500 |
| 열린 조개 | 열릴 때 1개 | 진주 | $350 |

1/3 구피는 재화를 생성하지 않는다. 2/3·성체 구피는 `threat`가 false일 때만 coinTimer를 진행한다. 포탈·에일리언이 존재하는 동안 타이머와 생산을 정지하고, 처치 후 저장한 시간에서 재개한다. 경고 단계에는 생산을 계속하며 전투 시간에 해당하는 재화를 나중에 몰아서 지급하지 않는다. 피라냐의 coinTimer와 조개 생산은 전투 중에도 진행한다. 진주를 제외한 재화는 초당 22씩 내려가 바닥에서 2초 후 제거된다.

수집은 활성 플레이에서만 가능하다. collected 또는 removed이면 거부하고, 유효하면 즉시 금액과 누적 수집액을 증가시킨다. 이후 약 0.45초 동안 목표 (1110,65)로 곡선 보간 이동한 뒤 제거한다.

해금은 살아 있는 구피의 최대 성장 단계로 검사하며 같은 스테이지에서는 되돌리지 않는다.

| 조건 | 결과 |
| --- | --- |
| 모든 구피가 1/3 | 모든 구매 항목 잠금 |
| 2/3 구피 달성 | 구피·사료 확장 해금 |
| 성체 구피 달성 | 알 해금, 1-2부터 피라냐도 해금 |
| 1-1 | 피라냐 항상 잠금 |

잠금과 잔액 부족을 서로 다른 CSS 상태로 표시한다. locked는 내용 전체를 숨기고 버튼을 비활성화한다. poor는 해금된 내용의 불투명도를 0.4로 낮추며, 눌렀을 때 부족 금액을 안내한다. 사료 상한 MAX는 추가 구매를 차단한다.

알 가격은 1-1 $300, 1-2 $1,000, 1-3 $2,500이다. `egg`는 실제 구매 횟수 0~3, `eggFrame = min(2, egg)`는 상점 이미지 프레임이다. 0회부터 이미지 1/3, 1회는 2/3, 2회는 3/3이며, 세 번째 구매는 즉시 부화장으로 전환한다.

`#egg-goal`은 `stage === 1 && !unlocks.egg`일 때만 숨긴다. 목표 노출과 구매 가능 여부는 독립적이다.

## 7. 에일리언과 펫

### 에일리언

1-1에서는 생성하지 않는다. 1-2부터 적이 없을 때 타이머를 증가시켜 55초에 경고하고 60초에 포탈을 만든다. 수중 물고기와의 최소 거리가 큰 위치를 최대 20개 후보에서 골라 즉시 포식 위험을 줄인다. 포탈은 1.2초간 9프레임을 재생한다.

| 설정 | 값 |
| --- | --- |
| HP | 30 |
| 이동 속도 | 120 좌표/초 |
| 물고기 포식 거리 | 35 미만 |
| 표시·클릭 크기 | `alienSize`: 226.2×223.6, 이전 가로·세로의 1.3배 |
| 플레이어 클릭 피해 | 1 |
| 앞쪽 공격 넉백 | 20 |
| 클릭 경직 | 0.13초 |
| 앞쪽 넉백 누적 4회 | 방향 전환·0.55초 후퇴 |

가장 가까운 살아 있는 수중 물고기를 추적한다. 펫은 먹지 않는다. 플레이어 또는 청새치의 유효 공격마다 hit 이벤트를 발생시킨다. HP가 0 이하이면 적을 제거하고 경고·등장 타이머를 초기화한다. 마지막 물고기를 잃으면 GAME_OVER가 되며 업데이트와 일반 입력을 막는다.

### 조개

1-2부터 바닥 Y597에 생성한다. CLOSED 20초 → OPENING 1초 → OPEN 5초 → CLOSING 1초 → CLOSED 순서다. `clamAnimationDuration = 1`로 개폐 애니메이션은 기존 0.5초 대비 절반 속도다. 닫힌 상태 및 부화장 보상 조개의 반복은 `clamAnimationFPS = 3.5`로 기존 7fps의 절반이다. 대기 20초와 진주 수집 5초는 유지한다. OPEN 진입 시 진주 하나를 (clam.x, clam.y-22)에 만들고 pearlId로 추적한다. 열린 시간이 끝나면 미수집 진주만 제거한다. 진주는 낙하하지 않는다.

### 청새치

1-3부터 생성한다. 평상시 속도는 48, 적 추적 속도는 215다. 에일리언과 거리 65 미만일 때 접촉 시간을 누적하여 1초마다 `marlinDamage = 3` 피해를 준다. 접촉이 끊기면 공격 타이머를 초기화한다. 청새치 공격에는 플레이어 넉백·경직을 적용하지 않는다.

## 8. 부화장·전환·초기화

3회째 알 구매는 HATCHERY로 전환한다. 이후 사료·수집·구매·공격 입력은 거부한다. 부화 시간은 2.4초이며 시간 구간에 따라 Egg 프레임 2 → 1 → 0을 사용한다. 0.8초 임계점을 지날 때 hatch 효과음을 정확히 한 번 발생시킨다. 큰 시간 간격에서도 임계점 통과를 확인한다.

1-1·1-2는 완료 후 조개·청새치를 표시하고 다음 스테이지 버튼을 활성화한다. 1-3은 ENDING으로 바뀌어 완료 메시지와 기록을 표시한다.

스테이지 변경 시 개체·입수·이펙트 DOM, 개체 Map, 누적 프레임 시간과 이전 안내를 정리한다. 새 스테이지는 $100·허기 구피 2마리·사료 상한 3·알 0·전체 잠금으로 시작한다. 펫은 스테이지 번호에 따라 생성한다. 홀드 입력은 그대로 반영한다.

## 9. 스프라이트·UI·반응형

3×3 스프라이트는 CSS background-size 300%와 프레임별 background-position을 사용한다. 알은 3×1, 사망 물고기는 1×1이다.

| 자산 | 처리 |
| --- | --- |
| Guppy_Default / Guppy_Starvation | 9프레임, 게임 시간 기준 7fps |
| Piranha_Default / Piranha_Starvation | 9프레임, 7fps |
| Guppy_Dead / Piranha_Dead | 단일 이미지, 추가 상하반전 없음 |
| Clam_Default | CLOSED 및 부화장 보상에서 9프레임 3.5fps 반복 |
| Clam_Open | 열기 0~3을 1초, 열린 상태 4를 5초, 닫기 5~8을 1초 |
| StripedMarlin_Default / StripedMarlin_Anger | 9프레임, 8fps |
| Alien | 9프레임, 9fps |
| Alien_Portal | 1.2초에 9프레임 |
| Effect_Hit / Effect_Carnivore | 약 0.55초에 9프레임 |
| Egg | 0=1/3, 1=2/3, 2=3/3 |
| Background_Stage / Background_Hatchery | 수족관·부화장 배경 |
| Prey / Money_SilverCoin / Money_GoldCoin / Money_Diamond / Money_Pearl | 사료·재화 |
| UI_ItemSlot / UI_MoneySlot | 상점·지갑 프레임 |

모든 이름에는 `.png` 확장자가 붙으며, 실제 청새치 기본 파일명은 `StripedMarlin_Default.png`다.

물고기는 이동 방향에 따라 좌우로 방향을 바꾸고, 살아 있을 때만 작은 유영 기울기를 적용한다. 별도의 성장 점 표시는 없다. 허기 말풍선만 상태에 따라 표시한다.

상점은 구피·사료 확장·피라냐·알 순서이며 구피 버튼은 너비 기준 오른쪽 7%, 알 버튼은 왼쪽 9% 이동한다. 구피는 이전의 6%에서 1%p 추가 조정했다. 잠긴 상태에서도 프레임 위치는 유지한다.

수족관의 16:9 비율을 유지하며 760px·480px·360px 너비 및 낮은 화면 높이에 맞춘 CSS가 있다. 모바일 가로 화면을 권장하고 세로 화면 안내도 제공한다. `prefers-reduced-motion`에서는 일부 장식 애니메이션을 줄인다. 배속 버튼은 touch-action:none과 포인터 캡처로 홀드 중 스크롤 간섭을 막는다.

## 10. 오디오

HTMLAudioElement를 사용한다. 기본 소리는 켜져 있지만 최초 pointerdown 또는 keydown 뒤에 재생한다. 오디오 객체는 `#game-audio`에 보관하며 preload:auto로 준비한다. 재생 거부 Promise를 처리하고 다음 사용자 입력에서 재시도한다.

| 매핑 키 | 파일 | 조건 |
| --- | --- | --- |
| music.default | `Background_Default.mp3` | 일반 플레이·부화장·엔딩 |
| music.alien | `Background_Alien.mp3` | STAGE에서 portal 또는 alien 존재 |
| portal | `AlienAppearance.mp3` | 포탈 생성 |
| splash | `FishDrop.mp3` | 구매 구피 수면 도달 |
| collect | `MoneyAcquisition.mp3` | 유효 재화 수집 |
| hit | `Hit.mp3` | 에일리언 피격 |
| chomp | `Carnibore.mp3` | 물고기 포식 |
| hatch | `EggDestruction.mp3` | 부화 시간 0.8초 통과 |
| ui | `UIClick.mp3` | 일반 UI 클릭·홀드 시작·일시정지 단축키 |

경로는 모두 `Sounds/` 기준이다. MoneyAcquisition과 EggDestruction은 실제 MP3 파일명과 config 매핑을 함께 변경한 이름이다.

배경음은 loop=true, 볼륨 **0.125**다. 효과음의 기본값은 `effectVolume = 0.7`이며 `effectVolumes`의 collect·ui·hit은 각각 **0.35**로 기존 음량의 50%를 적용한다. 모든 음성에 `effectVolumes[event] ?? effectVolume`을 설정한다. 이벤트별 4개 음성 풀을 사용하며 모두 사용 중이면 다음 음성을 처음부터 재사용한다. 배속과 관계없이 오디오 playbackRate는 기본 속도다.

일반·전투 음악은 상호 배타적으로 재생한다. 트랙이 바뀌면 새 트랙은 처음부터, 같은 트랙의 일시정지 해제는 멈춘 위치부터 재생한다. 로딩·일시정지·가이드·숨겨진 탭·게임오버에는 음악을 멈춘다. 정지 시 게임 효과음을 멈추되 UI 효과음은 조작 피드백으로 허용한다. 음소거·탭 숨김에는 UI를 포함해 모두 멈춘다. 매핑되지 않은 게임 이벤트에는 별도 합성음을 만들지 않는다.

## 11. 검증과 유지보수

Node.js 기본 테스트 러너로 실행한다. 추가 패키지는 필요하지 않다.

```sh
node --test tests/game.test.cjs tests/audio.test.cjs tests/input.test.cjs
```

현재 31개 테스트가 통과한다.

| 범위 | 검증 내용 |
| --- | --- |
| 게임 규칙 | 초기 조건, 허기·성장, 정상 상태 식사 차단, 사료·재화 중복 방지, 가격·가치, 해금·알 프레임 |
| 개체·전투 | 피라냐 먹이 제한, 구피 낙하·입수·정지, 포탈·전투 중 구피 생산 정지·재개, 조개 개폐 1초·진주 5초, 청새치 3 피해 |
| 진행 | 부화 시점, 3개 스테이지, 게임오버·재시작, 홀드 중 배속의 장면 간 유지 |
| 입력 | 누름 3배·해제 1배, 키 반복 방지, 버튼 밖 해제·취소, 포인터와 키보드 중첩, 포커스·탭 이탈 |
| 오디오 | BGM 0.125·collect/ui/hit 0.35·기타 효과음 0.7, 실제 MP3 경로, 전투 음악 전환, 음소거·재개, 중첩, 자동 재생 거부 |
| 캠페인 | 시작 $100에서 정상 먹이·수집·구매·공격만 사용해 1-3 엔딩 완료 |

기준 캠페인 시뮬레이션은 약 789초의 게임 시간 동안 에일리언 7마리를 처치한다. 자동 테스트는 모델·이벤트 처리를 검증하며 실제 음량의 청감이나 모든 기기의 표시 품질을 대신하지 않는다.

브라우저 확인용 서버는 다음처럼 실행한다.

```sh
node tests/preview.cjs
```

`http://127.0.0.1:4173`에서만 제공하며 게임 HTML·CSS·허용된 JS·PNG·MP3만 읽을 수 있다. 배포 또는 게임 실행에 필수인 서버는 아니다.

가격·주기·크기·배속·볼륨은 config에서 바꾸고, 변경 시 이 문서와 기획서·README의 관련 설명을 함께 갱신한다. 기능 변경에 맞는 기존 테스트를 먼저 수정하고 입력·장면 전환·중복 처리 같은 경계 조건을 확인한다.
